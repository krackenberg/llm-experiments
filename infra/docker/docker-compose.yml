version: "3.9"

services:
  llm-api:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - AWS_REGION=us-gov-west-1
      - AWS_PROFILE=default
    volumes:
      - ~/.aws:/root/.aws:ro
      - ../../traces:/app/traces
